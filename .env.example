# ============================================================================
# MOBO Discord Bot Environment Configuration
# Copy this file to .env and fill in your actual values
# ============================================================================

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Discord Bot Token (get from Discord Developer Portal)
DISCORD_TOKEN=your_discord_bot_token_here

# OpenRouter API Key (for LLM access)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Bot Personality Prompt (multiline text or base64 encoded)
PERSONALITY_PROMPT=You are a helpful and friendly Discord bot assistant.

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database URL (used by LangGraph PostgresSaver and PostgresStore)
DATABASE_URL=postgresql://mobo:mobo@localhost:5432/mobo

# Database connection settings
DATABASE_ECHO=false
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# =============================================================================
# LLM MODEL CONFIGURATION
# =============================================================================

# OpenRouter Base URL
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Main Chatbot Model (needs tool calling capabilities)
CHATBOT_MODEL=openai/gpt-4o
CHATBOT_TEMPERATURE=0.7

# =============================================================================
# EXTERNAL API KEYS (OPTIONAL)
# =============================================================================

# OpenAI API Key (for DALL-E image generation)
OPENAI_API_KEY=your_openai_api_key_here

# Giphy API Key (for GIF search)
GIPHY_API_KEY=your_giphy_api_key_here

# Google Custom Search API Key (for web search)
GOOGLE_CUSTOM_SEARCH_API_KEY=your_google_api_key_here

# Google Custom Search Engine ID (for web search)
GOOGLE_CSE_ID=your_custom_search_engine_id_here

# =============================================================================
# IMAGE GENERATION SETTINGS
# =============================================================================

# DALL-E Model
IMAGE_MODEL=dall-e-3

# Image Size (dall-e-2: 256x256, 512x512, 1024x1024 | dall-e-3: 1024x1024, 1536x1024, 1024x1536, 1792x1024, 1024x1792)
IMAGE_SIZE=1024x1024

# Image Quality (dall-e-3 only: standard, hd)
IMAGE_QUALITY=standard

# =============================================================================
# BOT BEHAVIOR CONFIGURATION
# =============================================================================

# Maximum consecutive bot-to-bot responses
MAX_BOT_RESPONSES=3

# Memory and RAG Configuration
SIMILARITY_THRESHOLD=0.7          # Minimum similarity for relevant conversations (0.0-1.0)
RECENT_MESSAGES_LIMIT=5           # Number of recent messages in context (1-20)
RELEVANT_MESSAGES_LIMIT=3         # Number of relevant messages in context (0-10)

# =============================================================================
# ADMIN AND DEVELOPMENT
# =============================================================================

# Admin User IDs (comma-separated Discord user IDs)
ADMIN_USER_IDS=123456789012345678,987654321098765432

# Logging Level
LOG_LEVEL=INFO

# Debug Mode
DEBUG_MODE=false

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
#
# 1. Required Variables:
#    - DISCORD_TOKEN: Must be set for Discord connectivity
#    - OPENROUTER_API_KEY: Must be set for LLM functionality
#    - PERSONALITY_PROMPT: Must be set for bot personality
#
# 2. Database Setup:
#    - Use "just dev" to start PostgreSQL in Docker for development
#    - PostgreSQL is used by LangGraph PostgresSaver and PostgresStore
#    - No need for pgvector extension - LangGraph handles all persistence
#
# 3. Model Configuration:
#    - CHATBOT_MODEL: Single model handles all interactions with tool calling
#    - Recommended: openai/gpt-4o for best tool calling performance
#    - Alternative: anthropic/claude-3.5-sonnet for complex reasoning
#
# 4. Optional Features:
#    - OPENAI_API_KEY: Only needed for DALL-E image generation
#    - GIPHY_API_KEY: Only needed for GIF search functionality
#    - GOOGLE_CUSTOM_SEARCH_API_KEY & GOOGLE_CSE_ID: Only needed for web search functionality
#
# 5. Docker Deployment:
#    - When using docker-compose, DATABASE_URL should use service name:
#      postgresql://mobo:mobo@postgres:5432/mobo
#
# 6. Security:
#    - Never commit .env files to version control
#    - Use strong, unique credentials for production deployments
#    - Regenerate API keys if compromised
