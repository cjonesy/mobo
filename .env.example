# ============================================================================
# MOBO Discord Bot Environment Configuration
# Copy this file to .env and fill in your actual values
# ============================================================================

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Discord Bot Token (get from Discord Developer Portal)
MOBO_DISCORD__TOKEN=your_discord_bot_token_here

# OpenRouter API Key (for LLM access)
MOBO_OPENROUTER__API_KEY=your_openrouter_api_key_here

# Bot Personality Prompt (multiline text or base64 encoded)
MOBO_PERSONALITY__PROMPT=You are a helpful and friendly Discord bot assistant.

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database URL (used by LangGraph PostgresSaver and PostgresStore)
MOBO_DATABASE_URL=postgresql://mobo:mobo@localhost:5432/mobo

# Database connection settings
MOBO_DATABASE_ECHO=false
MOBO_DATABASE_POOL_SIZE=10
MOBO_DATABASE_MAX_OVERFLOW=20

# =============================================================================
# LLM MODEL CONFIGURATION
# =============================================================================

# OpenRouter Base URL
MOBO_OPENROUTER__BASE_URL=https://openrouter.ai/api/v1

# Supervisor LLM Configuration (tool planning and orchestration)
MOBO_SUPERVISOR_LLM__MODEL=openai/gpt-4o
MOBO_SUPERVISOR_LLM__TEMPERATURE=0.7

# Response LLM Configuration (personality and creativity)
MOBO_RESPONSE_LLM__MODEL=openai/gpt-4.1
MOBO_RESPONSE_LLM__TEMPERATURE=0.8
# =============================================================================
# EXTERNAL API KEYS (OPTIONAL)
# =============================================================================

# OpenAI API Key (for DALL-E image generation)
MOBO_OPENAI_API_KEY=your_openai_api_key_here

# Giphy API Key (for GIF search)
MOBO_GIPHY__API_KEY=your_giphy_api_key_here

# Google Custom Search API Key (for web search)
MOBO_GOOGLE_SEARCH__API_KEY=your_google_api_key_here

# Google Custom Search Engine ID (for web search)
MOBO_GOOGLE_SEARCH__CSE_ID=your_custom_search_engine_id_here

# =============================================================================
# IMAGE GENERATION SETTINGS
# =============================================================================

# DALL-E Model
MOBO_IMAGE_GENERATION__MODEL=dall-e-3

# Image Size (dall-e-2: 256x256, 512x512, 1024x1024 | dall-e-3: 1024x1024, 1536x1024, 1024x1536, 1792x1024, 1024x1792)
MOBO_IMAGE_GENERATION__SIZE=1024x1024

# Image Quality (dall-e-3 only: standard, hd)
MOBO_IMAGE_GENERATION__QUALITY=standard

# =============================================================================
# BOT BEHAVIOR CONFIGURATION
# =============================================================================

# Maximum consecutive bot-to-bot responses
MOBO_BOT_INTERACTION__MAX_BOT_RESPONSES=3
MOBO_BOT_INTERACTION__BOT_RESPONSE_COOLDOWN_SECONDS=60

# Memory and RAG Configuration
MOBO_MEMORY__SIMILARITY_THRESHOLD=0.7          # Minimum similarity for relevant conversations (0.0-1.0)
MOBO_MEMORY__RECENT_MESSAGES_LIMIT=5           # Number of recent messages in context (1-20)
MOBO_MEMORY__RELEVANT_MESSAGES_LIMIT=3         # Number of relevant messages in context (0-10)

# =============================================================================
# ADMIN AND DEVELOPMENT
# =============================================================================

# Admin User IDs (comma-separated Discord user IDs)
MOBO_ADMIN__USER_IDS=123456789012345678,987654321098765432

# Logging Level
MOBO_LOG_LEVEL=INFO

# Debug Mode
MOBO_DEBUG_MODE=false

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
#
# 1. Required Variables:
#    - MOBO_DISCORD__TOKEN: Must be set for Discord connectivity
#    - MOBO_OPENROUTER__API_KEY: Must be set for LLM functionality
#    - PERSONALITY_PROMPT: Must be set for bot personality
#
# 2. Database Setup:
#    - Use "just dev" to start PostgreSQL in Docker for development
#    - PostgreSQL is used by LangGraph PostgresSaver and PostgresStore
#    - No need for pgvector extension - LangGraph handles all persistence
#
# 3. Model Configuration:
#    - MOBO_SUPERVISOR_LLM__MODEL: Supervisor model for tool planning and orchestration
#    - Recommended: openai/gpt-4o for best tool calling performance
#    - Alternative: anthropic/claude-3.5-sonnet for complex reasoning
#
# 4. Optional Features:
#    - OPENAI_API_KEY: Only needed for DALL-E image generation
#    - GIPHY_API_KEY: Only needed for GIF search functionality
#    - GOOGLE_CUSTOM_SEARCH_API_KEY & GOOGLE_CSE_ID: Only needed for web search functionality
#
# 5. Docker Deployment:
#    - When using docker-compose, DATABASE_URL should use service name:
#      postgresql://mobo:mobo@postgres:5432/mobo
#
# 6. Security:
#    - Never commit .env files to version control
#    - Use strong, unique credentials for production deployments
#    - Regenerate API keys if compromised
